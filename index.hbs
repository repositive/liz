{{!< default}}

{{> header}}

<div class="grid__wrap">
  <div class="grid">

    <section>
      {{> tag-nav }}
    </section>

    {{> divider }}

    {{#foreach posts}}

      {{> article-excerpt }}

    {{/foreach}}

    {{#if pagination}}
      <div class="u-ta--center">
        {{{pagination}}}
      </div>
    {{/if}}

  </div>
</div>
